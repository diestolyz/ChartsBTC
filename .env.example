# 复制为 .env 后按需修改（.env 勿提交仓库）
DB_HOST=127.0.0.1
DB_USER=poly
DB_PASSWORD=poly
DB_NAME=poly

# 可选：历史数据保留天数（默认 30）；清理间隔默认 24 小时（毫秒可改 DB_RETENTION_SWEEP_MS）
# DB_RETENTION_DAYS=30
# DB_RETENTION_SWEEP_MS=86400000
